"""Refine query use case."""


class RefineQueryUseCase:
    """Use case for query refinement."""

    def execute(self, query: str) -> str:
        """Return a refined version of the query.

        A full implementation will be added when query refinement is migrated.
        """
        return query.strip()
